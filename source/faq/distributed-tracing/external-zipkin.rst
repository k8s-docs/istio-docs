external-zipkin
==================================

To do so, you must you use the fully qualified domain name of the
Zipkin-compatible instance. For example:
``zipkin.mynamespace.svc.cluster.local``.
